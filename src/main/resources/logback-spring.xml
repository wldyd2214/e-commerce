<?xml version="1.0" encoding="UTF-8"?>
<configuration>

  <!-- (기존 appender 및 property 설정은 동일) -->
  <appender name="Console" class="ch.qos.logback.core.ConsoleAppender">
    <encoder>
      <pattern>[%d{yyyy-MM-dd HH:mm:ss}][%thread] %-5level %logger{36} - %msg%n</pattern>
    </encoder>
  </appender>

  <property name="LOG_PATH" value="./logs"/>
  <property name="LOG_FILE_NAME" value="api"/>
  <property name="ERR_LOG_FILE_NAME" value="api-error"/>

  <appender name="ApiLogFile" class="ch.qos.logback.core.rolling.RollingFileAppender">
    <filter class="ch.qos.logback.classic.filter.LevelFilter">
      <level>INFO</level>
      <onMatch>ACCEPT</onMatch>
      <onMismatch>DENY</onMismatch>
    </filter>
    <file>${LOG_PATH}/${LOG_FILE_NAME}.log</file>
    <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
      <pattern>[%d{yyyy-MM-dd HH:mm:ss}][%thread] %-5level %logger{36} - %msg%n</pattern>
    </encoder>
    <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
      <fileNamePattern>${LOG_PATH}/${LOG_FILE_NAME}.%d{yyyy-MM-dd}.gz</fileNamePattern>
      <maxHistory>30</maxHistory>
      <totalSizeCap>1GB</totalSizeCap>
    </rollingPolicy>
  </appender>

  <appender name="ErrorLogFile" class="ch.qos.logback.core.rolling.RollingFileAppender">
    <filter class="ch.qos.logback.classic.filter.LevelFilter">
      <level>ERROR</level>
      <onMatch>ACCEPT</onMatch>
      <onMismatch>DENY</onMismatch>
    </filter>
    <file>${LOG_PATH}/${ERR_LOG_FILE_NAME}.log</file>
    <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
      <pattern>[%d{yyyy-MM-dd HH:mm:ss}][%thread] %-5level %logger{36} - %msg%n</pattern>
    </encoder>
    <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
      <fileNamePattern>${LOG_PATH}/${ERR_LOG_FILE_NAME}.%d{yyyy-MM-dd}.gz</fileNamePattern>
      <maxHistory>30</maxHistory>
      <totalSizeCap>1GB</totalSizeCap>
    </rollingPolicy>
  </appender>

  <springProfile name="local">
    <logger name="com.hhplus.commerce.spring.support.log.LoggingInterceptor" level="INFO" additivity="false">
      <appender-ref ref="Console" />
      <appender-ref ref="ApiLogFile" />
    </logger>
  </springProfile>

  <springProfile name="dev">
    <logger name="com.hhplus.commerce.spring.support.log.LoggingInterceptor" level="INFO" additivity="false">
        <appender-ref ref="ApiLogFile"/>
    </logger>
  </springProfile>

  <springProfile name="prod">
    <logger name="com.hhplus.commerce.spring.support.log.LoggingInterceptor" level="INFO" additivity="false">
        <appender-ref ref="ApiLogFile"/>
    </logger>
  </springProfile>

  <!-- 모든 로그의 기본 설정을 담당하는 Root Logger 추가 -->
  <root level="INFO">
    <appender-ref ref="Console" />
    <appender-ref ref="ApiLogFile" />
    <appender-ref ref="ErrorLogFile" />
  </root>

</configuration>